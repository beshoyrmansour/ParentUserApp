export const config = {
    auth: {
        path: 'auth',
        route: 'auth',
        login: {
            path: 'login',
            route: 'auth/login'
        },
        signup: {
            path: 'signup',
            route: 'auth/signup'
        }
    }
}